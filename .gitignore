.DS_Store

# tpm-installed tmux plugins (regenerated by src/install-tmux-plugins.sh)
dotfiles/tmux/.tmux/plugins/

# lazygit runtime state (not configuration)
dotfiles/lazygit/Library/Application Support/lazygit/state.yml
# The same category as state.yml above. lazygit writes it per repository it has opened, keyed
# by absolute path, so it is a record of one machine's checkouts and can never be right on
# another.
dotfiles/lazygit/Library/Application Support/lazygit/github_pull_requests.json

# LLVM coverage profiling artifacts
*.profraw

# The stamped copies a plugin store under config moves a file aside to when it cannot read it,
# DisplayProfiles being the store that writes there. Workspaces used to keep its file here too,
# and it lives under the olm data root in the home directory now, so nothing of it is in the
# tree to ignore.
dotfiles/hammerspoon/.hammerspoon/config/*.json.*

# Herdr's own state, which used to land in the tracked tree because ~/.config/herdr was a
# single stow symlink back into this package, so everything herdr wrote beside its config was
# written here. The stow step now declares that directory must stay a real one and unfolds a
# machine where it is not, so nothing can be written here again once setup.sh has run. These
# three lines are transitional, they cover the machine that has not run it yet and they come
# out when every machine has. decisions/stow-folding.md carries the reasoning.
dotfiles/herdr/.config/herdr/plugins.json
dotfiles/herdr/.config/herdr/.plugins.lock
dotfiles/herdr/.config/herdr/herdr-server.log
