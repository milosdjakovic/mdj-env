# What this module needs on the machine, name | kind | locator | policy | consumer | reason.
# The format and the kinds are documented in the repository root CLAUDE.md. This file names
# tools and never how to install one, since that mapping is DEPENDENCIES.map one layer up.
# Written by hand, because this module has no sub units to fold declarations from.
#
# This module declares a tool and ships no configuration, which is unusual here and is
# explained in its CLAUDE.md. mole writes its own two config files through its own interactive
# commands, neither exists on this machine yet, and inventing them to give the package
# something to stow would be writing choices nobody made.
mole | path | mole | required | mole | the disk and system maintenance tool this module exists to declare, so a fresh machine gets it rather than finding it missing months later | brew | mole
