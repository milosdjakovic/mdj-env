# Repo only files at this package root, kept out of the home directory. The manifest is this
# module's contract with the layer above, so it belongs to the repository rather than to the
# running configuration. The module guidance is the same, notes for a person reading the
# repository rather than anything herdr loads.
CLAUDE.md
DEPENDENCIES
# The map and the emitter are this module's half of the theme contract, read by
# src/check-theme.sh and by nothing herdr loads.
theme-map
theme-emit
# The fold declaration is read by the stow step one layer up, before anything is linked, so it
# belongs to the repository rather than to the running configuration.
NO-FOLD
