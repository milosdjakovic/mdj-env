# BrowserTabs

Lists every open tab across whichever browsers are switched on, the tabs you
opened through this tool leading the list and everything else resting in its own
browser's ordinary order. Each row carries its browser's icon, choosing one
selects that tab, raises its window, and brings the browser forward. The last row
opens a settings page where each browser is switched on or off and shows whether
it is installed, running, and allowed to be automated.

Opens on Hyper and W, appears in the launcher under Tools, and can be searched
without leaving the launcher by typing t or tabs, which lists the tabs alone with
the settings row left out.

While the list is open, j and k move, i selects the highlighted row, and x closes
it.
