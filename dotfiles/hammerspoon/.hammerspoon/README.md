# Hammerspoon configuration

Open Hammerspoon Console from terminal run:

```bash
open -a Hammerspoon
```

Get the bundle identifier of the app:

```bash
osascript -e 'id of app "APP_NAME"'
```

> Replace `APP_NAME` with the name of the app.
