# What this module needs from outside itself. Generated by dependencies-collect,
# do not edit. Each line is name | kind | locator | policy | consumer | reason.
#
# This file is the whole contract with the layer above. It names tools and never
# names how to install one, because that mapping belongs to the layer that owns
# package management. The consumer column names the file that declared the line,
# so to change one, edit the declaration beside that file and run this again.
hammerspoon | app | org.hammerspoon.Hammerspoon | required | hammerspoon | the application this entire module configures
panel | core | lib/panel.lua | required | hammerspoon | the shared canvas surface behind every preview pane, docked hint bar, and cheat sheet
cheatsheet | core | lib/cheatsheet.lua | required | hammerspoon | the shared overlay renderer both cheat sheets draw through
chordkey | core | lib/chordkey.lua | required | hammerspoon | the shared hold and tap engine every leader key registers into
hyperkey | core | lib/hyperkey.lua | required | hammerspoon | the leader modal every Hyper binding and every Hyper context binds into
chooser | core | lib/chooser/init.lua | required | hammerspoon | the picker facade every list tool in this config is built on
deps | core | lib/deps.lua | required | hammerspoon | the declaration reader that hands every other spoon its resolved tools
osascript | system | /usr/bin/osascript | optional | BrowserTabs/jxa | runs the JXA that asks each browser for its open tabs
swiftc | system | /usr/bin/swiftc | optional | BrowserTabs/permissions | builds the helper that asks for automation permission per browser
jq | path | jq | optional | BrowserTabs/harness | reads every answer the harness gets back from the browsers and from the agent
osascript | system | /usr/bin/osascript | optional | BrowserTabs/harness | runs the browser adapters and the accessibility witness that judges each round
open | system | /usr/bin/open | optional | BrowserTabs/harness | carries each command to the harness agent as a URL, since the Hammerspoon CLI blocks while the tool has work in flight
ioreg | system | /usr/sbin/ioreg | optional | BrowserTabs/harness | asks the window server whether the screen is locked, which the accessibility layer cannot be trusted to report about itself
caffeinate | system | /usr/bin/caffeinate | optional | BrowserTabs/harness | holds the display awake for a run, since a suite drives the machine with synthesised events and the idle timer does not count those
ocr | path | ocr | optional | Capture/macocr | the OCR backend, the sole handler of text capture
macshot | app | com.sw33tlie.macshot.macshot | optional | Capture/macshot | the preferred screenshot backend, native is the fallback
displayplacer | path | displayplacer | optional | DisplayProfiles/engine | reading and applying display arrangements
fd | path | fd | optional | FileSearch/hidden | building the index of paths Spotlight cannot see
fzf | path | fzf | optional | FileSearch/hidden | ranking that index, the one thing a substring tool cannot do over 42 thousand matches
fd | path | fd | optional | FileSearch/walk | walking a named directory, which is faster than the index once a scope is given and is the only way to see dotfiles
qlmanage | system | /usr/bin/qlmanage | optional | FileSearch/thumbs | drawing a preview of a pdf, a video, or anything else only Quick Look can render
swiftc | system | /usr/bin/swiftc | optional | FileSearch/quicklook | compiling the helper that opens the native Quick Look panel
recency | core | lib/recency.lua | optional | browsertabs/browsertabs | remembering the last opened tab, so the list opens already ordered by that
osascript | system | /usr/bin/osascript | optional | browsertabs/jxa | runs the JXA that asks each browser for its open tabs
swiftc | system | /usr/bin/swiftc | optional | browsertabs/permissions | builds the helper that asks for automation permission per browser
jq | path | jq | optional | browsertabs/harness | reads every answer the harness gets back from the browsers and from the agent
osascript | system | /usr/bin/osascript | optional | browsertabs/harness | runs the browser adapters and the accessibility witness that judges each round
open | system | /usr/bin/open | optional | browsertabs/harness | carries each command to the harness agent as a URL, since the Hammerspoon CLI blocks while the tool has work in flight
ioreg | system | /usr/sbin/ioreg | optional | browsertabs/harness | asks the window server whether the screen is locked, which the accessibility layer cannot be trusted to report about itself
caffeinate | system | /usr/bin/caffeinate | optional | browsertabs/harness | holds the display awake for a run, since a suite drives the machine with synthesised events and the idle timer does not count those
ocr | path | ocr | optional | capture/macocr | the OCR backend, the sole handler of text capture
macshot | app | com.sw33tlie.macshot.macshot | optional | capture/macshot | the preferred screenshot backend, native is the fallback
paste | core | lib/paste.lua | required | clipboard/manager | the shared insertion engine every paste and every selection read runs through
ffmpeg | path | ffmpeg | optional | clipboard/preview | video clipboard previews
ffprobe | path | ffprobe | optional | clipboard/preview | reading a video duration so a preview frame is picked mid clip
qalc | path | qalc | required | convert | evaluating unit and currency conversions
displayplacer | path | displayplacer | optional | displayprofiles/engine | reading and applying display arrangements
jq | path | jq | optional | emoji/regenerate | reshaping both upstream sources into the vendored dataset
perl | path | perl | optional | emoji/regenerate | parsing the Unicode Character Database into candidate rows
hs | path | hs | optional | emoji/regenerate | rendering every candidate glyph to drop the ones that draw as a box, and writing the dataset
swiftc | system | /usr/bin/swiftc | optional | eyedropper | compiles the native colour sampler helper
fd | path | fd | optional | filesearch/hidden | building the index of paths Spotlight cannot see
fzf | path | fzf | optional | filesearch/hidden | ranking that index, the one thing a substring tool cannot do over 42 thousand matches
fd | path | fd | optional | filesearch/walk | walking a named directory, which is faster than the index once a scope is given and is the only way to see dotfiles
qlmanage | system | /usr/bin/qlmanage | optional | filesearch/thumbs | drawing a preview of a pdf, a video, or anything else only Quick Look can render
swiftc | system | /usr/bin/swiftc | optional | filesearch/quicklook | compiling the helper that opens the native Quick Look panel
ps | system | /bin/ps | optional | processes/metrics | samples cpu and memory for the rows the picker is showing
docker | path | docker | optional | processes/docker | lists running containers and stops one by name
lsof | system | /usr/sbin/lsof | optional | processes/ports | finds which process holds a listening port
ps | system | /bin/ps | optional | processes/ports | reads the command, start time, and working directory of a holder
kill | system | /bin/kill | optional | processes/ports | stops a holder, gracefully first and forcefully on request
ps | system | /bin/ps | optional | processes/runtimes | finds candidate runtime processes and reads their working directory
lsof | system | /usr/sbin/lsof | optional | processes/runtimes | checks whether a candidate holds a socket, the evidence that separates a dev server from a stray process
kill | system | /bin/kill | optional | processes/runtimes | stops a runtime process, gracefully first and forcefully on request
mullvad | path | mullvad | optional | vpn/mullvad | the relay CLI behind every VPN control and location
recency | core | lib/recency.lua | optional | vpn/vpn | remembering the last used city, so the list opens already ordered by that
